# Treesitter

Parse generate a `grammar.js` containing the syntax of the grammar
using treesiter grammar description javascript function.

The goal is not to generate a treesitter parse. Only the grammar.js
file needs to be generated.

- Parse ../grammar.txt and generate a grammar.js file following treesitter
  grammar description.
