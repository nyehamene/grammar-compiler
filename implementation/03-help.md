# Help

Print help message to stdout and exit with 0 exit code.

- Implement `grammaar help`.
- Implement `grammar fmt --help`.
- Implement `grammar check --help`.
- Implement `grammar print --help`.
- Implement `grammar diff --help`.
- Implement `grammar lsp --help`.
